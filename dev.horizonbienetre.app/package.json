{
    "devDependencies": {
        "nodemon": "^3.1.10"
    },
    "scripts": {
        "preprod": "set NODE_ENV=preprod && node server.js",
        "dev": "set NODE_ENV=dev && nodemon server.js",
        "start": "node server.js"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.5.0",
        "express": "^5.1.0"
    }
}
